import React, { useMemo, useState } from "react";
import { FiBell, FiActivity, FiHeart, FiAlertTriangle, FiCheckCircle, FiClock, FiPlus, FiPhoneCall, FiX
} from "react-icons/fi";
import "./index.css"; const initialReminders = [ { id: 1, name: "Paracetamol", time: "08:00 AM", status: "Taken" }, { id: 2, name: "Vitamin D", time: "01:00 PM", status: "Due now" }, { id: 3, name: "BP Tablet", time: "08:00 PM", status: "Upcoming" },
]; const tips = [ "Drink water regularly through the day.", "Walk for at least 20 minutes.", "Track BP and sugar after meals.",
]; function statusClass(status) { if (status === "Taken") return "badge success"; if (status === "Due now") return "badge warning"; if (status === "Missed") return "badge danger"; return "badge neutral";
} export default function App() { const [activeTab, setActiveTab] = useState("Dashboard"); const [showModal, setShowModal] = useState(false); const [showHistory, setShowHistory] = useState(false); const [showManage, setShowManage] = useState(false); const [reminders, setReminders] = useState(initialReminders); const [form, setForm] = useState({ name: "", time: "", status: "Upcoming", }); const stats = useMemo(() => { const taken = reminders.filter((r) => r.status === "Taken").length; const upcoming = reminders.filter((r) => r.status === "Upcoming").length; const alerts = reminders.filter((r) => r.status === "Due now" || r.status === "Missed").length; return [ { title: "Today's Reminders", value: reminders.length, icon: <FiBell /> }, { title: "Taken", value: taken, icon: <FiCheckCircle /> }, { title: "Upcoming", value: upcoming, icon: <FiClock /> }, { title: "Alerts", value: alerts, icon: <FiAlertTriangle /> }, ]; }, [reminders]); const handleAddReminder = (e) => { e.preventDefault(); if (!form.name.trim() || !form.time.trim()) { alert("Please enter medicine name and time."); return; } const newReminder = { id: Date.now(), name: form.name.trim(), time: form.time.trim(), status: form.status, }; setReminders((prev) => [newReminder, ...prev]); setForm({ name: "", time: "", status: "Upcoming" }); setShowModal(false); setActiveTab("Reminders"); }; const handleSOS = () => { alert("SOS triggered. Contacting emergency support..."); }; const markTaken = (id) => { setReminders((prev) => prev.map((item) => item.id === id ? { ...item, status: "Taken" } : item ) ); }; const removeReminder = (id) => { setReminders((prev) => prev.filter((item) => item.id !== id)); }; return ( <div className="app-shell"> <aside className="sidebar"> <div className="brand"> <div className="brand-mark"> <FiHeart /> </div> <div> <h2>Health Tracker</h2> <p>Care dashboard</p> </div> </div> <nav className="nav-links"> {["Dashboard", "Reminders", "Records", "SOS"].map((tab) => ( <button key={tab} className={`nav-item ${activeTab === tab ? "active" : ""}`} onClick={() => { setActiveTab(tab); setShowHistory(false); }} > {tab === "Dashboard" && <FiActivity />} {tab === "Reminders" && <FiBell />} {tab === "Records" && <FiHeart />} {tab === "SOS" && <FiAlertTriangle />} {tab} </button> ))} </nav> </aside> <main className="main-content"> <header className="topbar"> <div> <p className="eyebrow">Good afternoon</p> <h1>Welcome back, Sandeep</h1> <p className="subtext">Here’s your health summary for today.</p> </div> <div className="topbar-actions"> <button className="btn btn-secondary" onClick={() => { setShowHistory((prev) => !prev); setActiveTab("Records"); }} > View history </button> <button className="btn btn-primary" onClick={() => setShowModal(true)}> <FiPlus /> Add reminder </button> </div> </header> <section className="stats-grid"> {stats.map((item) => ( <article className="card stat-card" key={item.title}> <div className="stat-icon">{item.icon}</div> <div> <p className="muted">{item.title}</p> <h3>{item.value}</h3> </div> </article> ))} </section> {activeTab === "Dashboard" || activeTab === "Reminders" ? ( <section className="content-grid"> <article className="card panel"> <div className="panel-head"> <div> <p className="eyebrow">Medication</p> <h2>Today's reminders</h2> </div> <button className="btn btn-ghost" onClick={() => setShowManage((prev) => !prev)} > {showManage ? "Close" : "Manage"} </button> </div> <div className="reminder-list"> {reminders.map((item) => ( <div className="reminder-item" key={item.id}> <div> <h3>{item.name}</h3> <p className="muted">{item.time}</p> </div> <div className="reminder-actions"> <span className={statusClass(item.status)}>{item.status}</span> {showManage && ( <div className="inline-actions"> <button className="mini-btn" onClick={() => markTaken(item.id)}> Mark taken </button> <button className="mini-btn danger-outline" onClick={() => removeReminder(item.id)}> Delete </button> </div> )} </div> </div> ))} </div> </article> <article className="card panel"> <div className="panel-head"> <div> <p className="eyebrow">Wellness</p> <h2>Health tips</h2> </div> </div> <div className="tips-list"> {tips.map((tip, index) => ( <div className="tip-item" key={index}> <span className="tip-dot"></span> <p>{tip}</p> </div> ))} </div> </article> <article className="card panel sos-panel"> <div className="panel-head"> <div> <p className="eyebrow danger-text">Emergency</p> <h2>SOS support</h2> </div> </div> <p className="muted"> Use this button only for urgent medical support or emergency contact access. </p> <button className="btn btn-danger sos-btn" onClick={handleSOS}> <FiPhoneCall /> Trigger SOS </button> </article> </section> ) : null} {activeTab === "Records" && ( <section className="card panel"> <div className="panel-head"> <div> <p className="eyebrow">Records</p> <h2>Reminder history</h2> </div> </div> <p className="muted"> {showHistory ? "History view is active. You can later connect this to backend records." : "This section can show medicine logs, taken history, and missed alerts."} </p> <div className="tips-list" style={{ marginTop: "16px" }}> {reminders.map((item) => ( <div className="tip-item" key={item.id}> <span className="tip-dot"></span> <p> {item.name} - {item.time} - {item.status} </p> </div> ))} </div> </section> )} {activeTab === "SOS" && ( <section className="card panel sos-panel"> <div className="panel-head"> <div> <p className="eyebrow danger-text">Emergency</p> <h2>SOS actions</h2> </div> </div> <p className="muted"> This section can later connect to emergency contacts, ambulance support, or caretaker notification. </p> <button className="btn btn-danger sos-btn" onClick={handleSOS}> <FiPhoneCall /> Trigger SOS </button> </section> )} </main> {showModal && ( <div className="modal-backdrop" onClick={() => setShowModal(false)}> <div className="modal-card" onClick={(e) => e.stopPropagation()}> <div className="modal-head"> <div> <p className="eyebrow">New entry</p> <h2>Add reminder</h2> </div> <button className="icon-btn" onClick={() => setShowModal(false)}> <FiX /> </button> </div> <form className="modal-form" onSubmit={handleAddReminder}> <label> Medicine name <input type="text" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Enter medicine name" /> </label> <label> Time <input type="text" value={form.time} onChange={(e) => setForm({ ...form, time: e.target.value })} placeholder="Eg: 09:30 PM" /> </label> <label> Status <select value={form.status} onChange={(e) => setForm({ ...form, status: e.target.value })} > <option>Upcoming</option> <option>Due now</option> <option>Taken</option> <option>Missed</option> </select> </label> <div className="modal-actions"> <button type="button" className="btn btn-secondary" onClick={() => setShowModal(false)}> Cancel </button> <button type="submit" className="btn btn-primary"> Save reminder </button> </div> </form> </div> </div> )} </div> );
} 
